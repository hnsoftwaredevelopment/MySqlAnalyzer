<ResourceDictionary
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:converters="clr-namespace:MySqlAnalyzer.Converters"
	xmlns:local="clr-namespace:Syncfusion.UI.Xaml.Grid;assembly=Syncfusion.SfGrid.WPF"
	xmlns:syncfusion="http://schemas.syncfusion.com/wpf"
	>

	<converters:FilterIconConverter x:Key="FilterIconConverter" />
	<converters:AllowFilteringToVisibilityConverter x:Key="AllowFilteringToVisibilityConverter" />
	<local:SortDirectionToVisibilityConverter x:Key="sortDirectionToVisibilityConverter" />
	<local:SortDirectionToWidthConverter x:Key="sortDirectionToWidthConverter" />
	<Style TargetType="{x:Type local:GridHeaderCellControl}">
		<Setter Property="Background" Value="Cornsilk" />
		<Setter Property="BorderBrush" Value="DarkBlue" />
		<Setter Property="BorderThickness" Value="0,0,.5,.5" />
		<Setter Property="FontSize" Value="14" />
		<Setter Property="FontWeight" Value="Bold" />
		<Setter Property="Foreground" Value="DarkBlue" />
		<Setter Property="HorizontalContentAlignment" Value="Center" />
		<Setter Property="IsTabStop" Value="False" />
		<Setter Property="Padding" Value="5,3,5,3" />
		<Setter Property="Template">
			<Setter.Value>
				<ControlTemplate TargetType="{x:Type local:GridHeaderCellControl}">
					<Grid>
						<Border
							x:Name="PART_FrozenCellBorder"
							Background="Transparent"
							BorderBrush="{TemplateBinding BorderBrush}"
							/>
						<Border
							x:Name="PART_FooterCellBorder"
							Background="Transparent"
							BorderBrush="{TemplateBinding BorderBrush}"
							/>
						<Border
							x:Name="PART_HeaderCellBorder"
							Background="{TemplateBinding Background}"
							BorderBrush="{TemplateBinding BorderBrush}"
							BorderThickness="{TemplateBinding BorderThickness}"
							SnapsToDevicePixels="True"
							>
							<Grid Margin="{TemplateBinding Padding}" SnapsToDevicePixels="True">
								<Grid.ColumnDefinitions>
									<ColumnDefinition Width="*" />
									<ColumnDefinition Width="Auto" />
									<ColumnDefinition Width="Auto" />
								</Grid.ColumnDefinitions>
								<ContentPresenter
									HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
									VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
									Focusable="False"
									/>
								<Border x:Name="PART_FilterPopUpPresenter" />
								<Grid
									x:Name="PART_SortButtonPresenter"
									Grid.Column="1"
									SnapsToDevicePixels="True"
									>
									<Grid.ColumnDefinitions>
										<ColumnDefinition Width="0" MinWidth="{Binding SortDirection, Converter={StaticResource sortDirectionToWidthConverter}, Mode=OneWay, RelativeSource={RelativeSource Mode=TemplatedParent}}" />
										<ColumnDefinition Width="*" />
									</Grid.ColumnDefinitions>
									<Path
										Width="14"
										Height="14"
										HorizontalAlignment="Center"
										VerticalAlignment="Center"
										Data="{StaticResource Down}"
										Fill="DarkBlue"
										SnapsToDevicePixels="True"
										Stretch="Fill"
										Visibility="{Binding SortDirection, ConverterParameter=Ascending, Converter={StaticResource sortDirectionToVisibilityConverter}, RelativeSource={RelativeSource Mode=TemplatedParent}}"
										/>
									<Path
										Width="14"
										Height="14"
										HorizontalAlignment="Center"
										VerticalAlignment="Center"
										Data="{StaticResource Up}"
										Fill="DarkBlue"
										SnapsToDevicePixels="True"
										Stretch="Fill"
										Visibility="{Binding SortDirection, ConverterParameter=Decending, Converter={StaticResource sortDirectionToVisibilityConverter}, RelativeSource={RelativeSource Mode=TemplatedParent}}"
										/>
									<TextBlock
										Grid.Column="1"
										Margin="0,-4,0,0"
										VerticalAlignment="Center"
										Foreground="{TemplateBinding Foreground}"
										SnapsToDevicePixels="True"
										Text="{TemplateBinding SortNumber}"
										TextBlock.FontSize="10"
										Visibility="{TemplateBinding SortNumberVisibility}"
										/>
								</Grid>
								<!--  Filter Icon as Image  -->
								<Image
									x:Name="FilterIcon"
									Grid.Column="2"
									Width="14"
									Height="14"
									HorizontalAlignment="Center"
									VerticalAlignment="Center"
									Source="{StaticResource Filter}"
									Visibility="{Binding Column.AllowFiltering, Converter={StaticResource AllowFilteringToVisibilityConverter}, RelativeSource={RelativeSource AncestorType=syncfusion:GridHeaderCellControl}}"
									/>
							</Grid>
						</Border>
					</Grid>
				</ControlTemplate>
			</Setter.Value>
		</Setter>
		<Setter Property="VerticalContentAlignment" Value="Center" />
		<Setter Property="local:VisualContainer.WantsMouseInput" Value="True" />
	</Style>

	<Style TargetType="syncfusion:GridFilterRowCell">
		<Setter Property="Template">
			<Setter.Value>
				<ControlTemplate TargetType="syncfusion:GridFilterRowCell">
					<Grid>
						<Border
							Background="{TemplateBinding Background}"
							BorderBrush="{TemplateBinding BorderBrush}"
							BorderThickness="{TemplateBinding BorderThickness}"
							SnapsToDevicePixels="True"
							>

							<Grid Margin="5,3,5,3">
								<Grid.ColumnDefinitions>
									<ColumnDefinition Width="*" />
									<ColumnDefinition Width="Auto" />
								</Grid.ColumnDefinitions>

								<!--  Inhoud van de filtercel (bijv. TextBox)  -->
								<ContentPresenter
									Margin="0,0,5,0"
									HorizontalAlignment="Stretch"
									VerticalAlignment="Center"
									Focusable="False"
									SnapsToDevicePixels="True"
									/>

								<StackPanel
									Grid.Column="1"
									Orientation="Horizontal"
									Visibility="{Binding DataContext.Column.AllowFiltering, Converter={StaticResource AllowFilteringToVisibilityConverter}, RelativeSource={RelativeSource AncestorType=syncfusion:GridFilterRowCell}}"
                                
                                >
									<TextBox
										x:Name="SearchTextBox"
										Width="100"
										Margin="0,0,5,0"
										/>
									<Image
										x:Name="searchIcon"
										Width="14"
										Height="14"
										HorizontalAlignment="Center"
										VerticalAlignment="Center"
										Source="{StaticResource search}"
										/>
								</StackPanel>
							</Grid>
						</Border>
					</Grid>
				</ControlTemplate>
			</Setter.Value>
		</Setter>
	</Style>
</ResourceDictionary>